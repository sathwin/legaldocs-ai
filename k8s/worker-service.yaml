apiVersion: v1
kind: Service
metadata:
  name: legaldocs-worker
  namespace: legaldocs-ai
spec:
  selector:
    app: legaldocs-worker
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
